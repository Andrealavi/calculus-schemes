% preamble.tex
% Preamble for academic cascade scheme documents

\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage[english]{babel}
\usepackage{geometry}
\geometry{a4paper, margin=2.5cm}

\usepackage{amssymb}
\usepackage{enumitem}
\usepackage{tikz}
\usetikzlibrary{shapes, arrows.meta, positioning}
\usepackage{fancyhdr}
\usepackage{hyperref}
\hypersetup{
	colorlinks=true,
	linkcolor=black,
	urlcolor=black,
	citecolor=black
}

\usepackage{titlesec}

\titleformat{\section}
{\normalfont\Large\bfseries\color{black}}
{\thesection}{1em}{}
\titleformat{\subsection}
{\normalfont\large\bfseries\color{black}}
{\thesubsection}{1em}{}

\setlist[itemize]{left=1.3em, label=\textbullet, topsep=2pt, itemsep=2pt}
\setlist[enumerate]{left=1.3em, topsep=2pt, itemsep=2pt}

\usepackage{tcolorbox}
\tcbuselibrary{skins, breakable}

\definecolor{cascadegray}{RGB}{245,245,245} % very light gray
\definecolor{cascadegrayframe}{RGB}{80,80,80} % dark gray

\newtcolorbox{cascade}[1][]{
	enhanced,
	colback=cascadegray,
	colframe=cascadegrayframe,
	coltitle=cascadegray,
	fonttitle=\bfseries,
	title=#1,
	boxrule=0.6mm,
	arc=2mm,
	left=2mm,
	right=2mm,
	top=1mm,
	bottom=1mm,
	breakable
}

% Command for cascade scheme with TikZ (for visual hierarchies)
\newcommand{\cascadeschema}[1]{
	\begin{center}
		\begin{tikzpicture}[node distance=1.2cm, every node/.style={font=\small, align=left}]
			#1
		\end{tikzpicture}
	\end{center}
}

% Arrow for cascade schemes
\tikzset{cascadearrow/.style={-Latex, thick, black!80}}
